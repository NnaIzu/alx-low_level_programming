#include "main.h"

/**
 * add - sums up two integers
 * @i: first integers to be summed up
 * @k: second integer to be summed with the first
 * Return: value of the addition
 */

int add(int i, int k)
{
	return (i + k);
}
