#include "main.h"

/**
 * print_most_numbers - prints numnber
 *
 * Return: (void)
 */

void print_most_numbers(void)
{
	char n;

	for (n = '0'; n <= '9'; n++)
	{
		if (!(c == '2' || c == '4'))
		_putchar(n);
	}
	_putchar('\n');
}

