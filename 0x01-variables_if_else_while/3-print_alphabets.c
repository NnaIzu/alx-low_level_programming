#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char y;
	char b;

	y = 'a';
	b = 'A';
	while
		(y <= 'z') {
			putchar(y);
			y++;
		}
	while
		(b <= 'Z') {
			putchar (b);
			b++;
		}
	putchar('\n');
	return (0);
}
